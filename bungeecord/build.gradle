// BungeeCord 代理端平台模块。
// 功能与 velocity 模块对等，核心逻辑在 common-core（平台无关）。

dependencies {
    implementation project(':common-core')

    compileOnly 'net.md-5:bungeecord-api:1.20-R0.2-SNAPSHOT'

    // 读取 bungeecord 端配置；打包时由 bundle 重定位
    implementation 'org.yaml:snakeyaml:1.33'
}
